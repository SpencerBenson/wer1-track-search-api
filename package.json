{
  "name": "track-search-wer1-api",
  "version": "1.0.0",
  "description": "1. Clone the repository: `git clone https://github.com/yourusername/track-search-graphql-api.git`\r 2. Install dependencies: `npm install`\r 3. Create a `.env` file based on the provided `.env.example` and fill in the necessary credentials.",
  "main": "jest.config.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf ./buld && tsc",
    "start": "node build/src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "@prisma/client": "^5.9.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "nodemon": "^3.0.3",
    "prisma": "^5.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
